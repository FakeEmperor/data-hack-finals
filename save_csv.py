from typing import List
from pathlib import Path
from dno.proto.mock import MockInterop


def main(tasks: List[str]=None):
    """
    """
    Path().glob()
    tasks = [path.name for path in ]



if __name__ == "__main__":
    main()
